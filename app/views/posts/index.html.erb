<div class="container">
<div class="jumbotron jumbotron-fluid ">
  <div class="container">
    <h1 class="display-4">My Blog</h1>
    <p class="lead">This is Simple Blog app on ruby on rails.</p>
  </div>
</div>
</div>

<h1> Blog Post </h1>
    
    <% @posts.each do |post|%>
    <h3><%= post.title%></h3>
    <p><%=post.body%></p>
    <%= link_to  "Read More", post_path(post), :class => 'btn btn-default' %>
   <%end%> 